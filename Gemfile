source "https://rubygems.org"

# if (RUBY_PLATFORM == "x64-mingw-ucrt" || RUBY_PLATFORM == "x64-mingw32")
#   gemspec :name => "rest_client_windows"
#   # gemspec :name => 'rest-client.windows'
# else
#   gemspec :name => 'rest-client'
# end

group :test do
  gem 'rake'
end

gemspec